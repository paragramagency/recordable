# Copy to .env in this directory (auto-loaded from the script's dir). Secrets only.
# Only the key is secret — provider/voiceId/model live in demo.md frontmatter
# (or a recordable.config.json `voiceover` block).
# For silent audio, set the voiceover provider to `mock` instead of supplying a key.
ELEVENLABS_API_KEY=
