# Copy to .env in this directory (auto-loaded from the script's dir).
# Only the key is secret — provider/voiceId/model live in demo.md frontmatter.
# Set RECORDABLE_TTS_PROVIDER=mock instead of a key for silent audio.
ELEVENLABS_API_KEY=
